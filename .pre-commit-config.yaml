repos:
- repo: local
  hooks:
  - id: flake8
    name: flake8
    stages: [commit]
    language: system
    entry: pipenv run flake8
    types: [python]
  - id: pytest
    name: pytest
    stages: [commit]
    language: system
    entry: pipenv run test
    types: [python]
