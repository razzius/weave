import React from "react"
import AppScreen from "./AppScreen"

const LoginCheckEmail = () => (
  <AppScreen>
    <h1>Login email sent</h1>
    <p>Check your email and follow the link from there to log in.</p>
  </AppScreen>
)

export default LoginCheckEmail
